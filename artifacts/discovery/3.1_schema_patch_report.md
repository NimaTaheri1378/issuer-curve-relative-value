# 3.1 Schema patch report

- Run UTC: `2026-05-27T16:30:42.546586+00:00`
- Output config: `configs/schema_map.yaml`

## Corrections applied

| Field | Action |
|---|---|
| `fisd.coupon` | Set to `null`; `coupon_type` is categorical, not numeric coupon. |
| `fisd.callable_flag` | Set to `redeemable` as conservative callable proxy. |
| `returns.price` | Set to `price_eom` instead of static `offering_price`. |
| `returns_fallback.excess_return` | Set to `ret_exc`. |
| `link.cusip` | Set to `null`; `fang_link` uses issuer CUSIP, not bond CUSIP. |

## Next script

Run `bash run_4.0.sh` to extract and screen FISD issue master.
